package com.senniaf.match.model.enums;

/**
 * Qué tan seguido podría la familia viajar o recibir visitas si el niño
 * o niña está en otra región. Se enmarca como logística de
 * acompañamiento, no como límite de conveniencia (Sprint 0).
 */
public enum DisponibilidadViaje {
    ALTA,
    MEDIA,
    BAJA
}
