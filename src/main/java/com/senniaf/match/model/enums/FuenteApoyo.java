package com.senniaf.match.model.enums;

/**
 * Con quién cuenta la familia para apoyarse en el día a día (selección
 * múltiple). Reemplaza la pregunta binaria "¿Trabaja tiempo completo?"
 * por una medida más real de red de apoyo (Sprint 0, rediseño humanizado
 * del intake).
 */
public enum FuenteApoyo {
    PAREJA,
    FAMILIA_EXTENDIDA,
    AMISTADES_O_COMUNIDAD,
    NINGUNA_POR_AHORA
}
